{
  "scripts": {
    "start": "parcel serve src/index.html",
    "build": "parcel build src/index.html",
    "test": "tsc && prettier --check .",
    "fmt": "prettier --write ."
  },
  "devDependencies": {
    "parcel": "nightly",
    "prettier": "^2.1.2",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "three": "^0.120.1"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true
  }
}
